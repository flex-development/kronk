/**
 * @file Interfaces - ArgumentData
 * @module kronk/interfaces/ArgumentData
 */

import type { ParseableInfo } from '@flex-development/kronk'

/**
 * Data transfer object for command-arguments.
 *
 * @see {@linkcode ParseableInfo}
 *
 * @extends {ParseableInfo}
 */
interface ArgumentData extends ParseableInfo {}

export type { ArgumentData as default }
