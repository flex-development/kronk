/**
 * @file Unit Tests - kKronkEvent
 * @module kronk/internal/tests/unit/kKronkEvent
 */

import testSubject from '#internal/k-kronk-event'

describe('unit:internal/kKronkEvent', () => {
  it('should have the description "kKronkEvent"', () => {
    expect(testSubject).to.have.property('description', 'kKronkEvent')
  })
})
