/**
 * @file Unit Tests - kOption
 * @module kronk/internal/tests/unit/kOption
 */

import testSubject from '#internal/k-option'

describe('unit:internal/kOption', () => {
  it('should have the description "kOption"', () => {
    expect(testSubject).to.have.property('description', 'kOption')
  })
})
