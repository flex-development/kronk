/**
 * @file Unit Tests - kArgument
 * @module kronk/internal/tests/unit/kArgument
 */

import testSubject from '#internal/k-argument'

describe('unit:internal/kArgument', () => {
  it('should have the description "kArgument"', () => {
    expect(testSubject).to.have.property('description', 'kArgument')
  })
})
