/**
 * @file Unit Tests - kOptionEvent
 * @module kronk/internal/tests/unit/kOptionEvent
 */

import testSubject from '#internal/k-option-event'

describe('unit:internal/kOptionEvent', () => {
  it('should have the description "kOptionEvent"', () => {
    expect(testSubject).to.have.property('description', 'kOptionEvent')
  })
})
